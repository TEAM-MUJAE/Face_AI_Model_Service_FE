import { StyleSheet } from 'react-native';


const insertImageStyles = StyleSheet.create({
    imageInsertContainer: {
        backgroundColor: "red"
    },
    imageSection: {
        resizeMode: "contain"
    }
});

export default insertImageStyles;